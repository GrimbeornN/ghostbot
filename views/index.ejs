
<!DOCTYPE html>
<html>
<head>
  <title>GhostBot Primedice</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white font-mono p-6">
  <div class="max-w-5xl mx-auto">
    <div class="mb-6 flex justify-between items-center">
      <h1 class="text-2xl font-bold">GhostBot Primedice (Node.js)</h1>
      <form action="/setmode" method="POST" class="flex items-center space-x-2">
        <label>Mode:</label>
        <select name="mode" class="bg-gray-800 text-white p-1 rounded">
          <option value="simu" <%= mode === 'simu' ? 'selected' : '' %>>Simulation</option>
          <option value="live" <%= mode === 'live' ? 'selected' : '' %>>Live API</option>
        </select>
        <button class="bg-blue-500 px-3 py-1 rounded" type="submit">Changer</button>
      </form>
    </div>

    <div class="grid grid-cols-3 gap-4 mb-6">
      <div class="bg-gray-800 p-4 rounded shadow">Balance<br><span class="text-xl"><%= stats.balance.toFixed(8) %></span></div>
      <div class="bg-gray-800 p-4 rounded shadow">Profit<br><span class="text-xl"><%= (stats.balance - stats.startBalance).toFixed(8) %></span></div>
      <div class="bg-gray-800 p-4 rounded shadow">Drawdown Max<br><span class="text-xl"><%= stats.drawdownMax.toFixed(2) %>%</span></div>
    </div>

    <div class="mb-6">
      <span class="inline-block px-4 py-2 rounded font-bold text-sm 
                   <%= mode === 'live' ? 'bg-green-600' : 'bg-gray-700' %>">
        MODE ACTIF: <%= mode.toUpperCase() %>
      </span>
    </div>

    <a href="/reset" class="bg-yellow-400 text-black px-4 py-2 rounded">Reset</a>

    <div class="mt-6 bg-gray-800 p-4 rounded shadow h-64 overflow-y-scroll text-sm whitespace-pre-line">
      <% stats.log.slice(0, 50).forEach(function(line) { %>
        <%= line %>
      <% }) %>
    </div>
  </div>
</body>
</html>
